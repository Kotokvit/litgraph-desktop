# Python зависимости для NER + POLER модулей LitGraph
#
# Установка:
#   pip install -r src-tauri/python/requirements.txt
#   python -m spacy download ru_core_news_sm   # только для extract_svo (dep parsing)
#   python -m ruwordnet download
#
# Phase 1B: v1 ner_extract.py удалён. Все NER-пути используют v2 (Natasha).
# spaCy по-прежнему нужен для svo_extract.py (dependency parsing: token.children,
# token.dep_, token.head) — Natasha не даёт эквивалентного API.

# === Базовый слой ===
# natasha — основной NER-движок v2 (Slovnet NER + MorphTagger)
natasha>=1.4.0
# pymorphy3 — морфология (лемматизация, определение пола)
pymorphy3>=2.0.0
# spacy — только для svo_extract.py dependency parsing
spacy>=3.8.0,<4.0.0

# Линейная алгебра и кластеризация (poler_entities.py)
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0

# === Yargy (rule-based extraction, пока R&D-only) ===
yargy>=0.16.0
ipymarkup>=0.0.5

# === Графовые алгоритмы (community, centrality, paths) ===
networkx>=3.4
igraph>=0.11
pyvis>=0.3

# === Семантические векторы без трансформеров (R&D-only) ===
fasttext-wheel>=0.2.5
gensim>=4.3

# === Русские лингвистические ресурсы (R&D-only) ===
ruwordnet>=0.0.6
# RuSentiLex скачивается как файл: scripts/dev/resources/rusentilex_2017.txt
# Источник: http://www.labinform.ru/pub/rusentilex/rusentilex_2017.txt

# === Тестирование и разработка алгоритмов ===
hypothesis>=6.115
pytest>=8.0
rapidfuzz>=3.0
jupyterlab>=4.2

# Модели ставятся отдельно:
#   python -m spacy download ru_core_news_sm   # для SVO dep parsing
#   python -m ruwordnet download               # для semantic_vectors.py
